# Dependencies
node_modules/
.pnpm-store/

# Build output
.svelte-kit/
build/
dist/
.vercel/
.output/
.netlify/

# Turborepo
.turbo/

# Tauri / Rust
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Bundled engine binaries (large; downloaded as a Tauri sidecar at build time,
# or managed at runtime — see binaries/README.md). Covers the plain dev binary
# (tectonic[.exe]) and the per-target-triple sidecars (tectonic-<triple>[.exe]).
apps/desktop/src-tauri/binaries/tectonic*
apps/desktop/src-tauri/binaries/*.tar.gz
apps/desktop/src-tauri/binaries/*.tar.xz
apps/desktop/src-tauri/binaries/*.zip

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
